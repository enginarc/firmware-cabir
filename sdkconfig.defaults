# Bluetooth
CONFIG_BT_ENABLED=y
CONFIG_BT_NIMBLE_ENABLED=y

# Disable Bluedroid (not needed)
CONFIG_BT_BLUEDROID_ENABLED=n

# NimBLE host memory
CONFIG_BT_NIMBLE_MEM_ALLOC_MODE_INTERNAL=y

# Optional: increase log verbosity during dev
CONFIG_LOG_DEFAULT_LEVEL_INFO=y